export interface idea {
    topic: string,
    title: string,
    description: string,
    id: number
}

export interface logindata {
    email :string,
    password:number,
}

export interface registerdata {
    name: string,
    email: string,
    password: string,
}